# Task 6 report

## Scope

- Replaced wizard-era browser acceptance with one-selection exact, fallback/failure, reload/privacy, resource, and universal-output coverage.
- Added opt-in real-file browser acceptance through `KNIGHT_FORTRESS_STL` and `KNIGHT_FORTRESS_GROUP_STL`; neither fixture is copied, logged, or committed.
- Added ZIP entry, provenance, layer identity, finite contour, and one-contour-per-layer reconciliation.
- Updated the README and laser-safety guidance for the material-independent 2.5D workflow.
- Cross-scope metadata addition: propagated the extraction's measured `removedComponentCount` through `AutomaticOutlineResult`, canonical document, project JSON, manifest JSON, ZIP verification, and tests. The value is not inferred or generated by the UI.

## TDD evidence

- RED: the first rewritten browser run failed 4/4 against the former selector contract; after aligning to the approved accessible selector, exact ZIP reconciliation exposed the nested project-document schema and the open fixture proved the specified fail-closed `NO_OUTLINE` path.
- RED: the new export test observed `removedComponentCount` as `undefined` before production metadata propagation.
- GREEN: focused pipeline/export verification passed 394/394 after canonical propagation and reconciliation.

## Real fixture evidence

Direct pipeline smoke (temporary test removed immediately after execution):

| Fixture | Mode | Status | Layers | Removed components | Runtime | Source hash |
| --- | --- | --- | ---: | ---: | ---: | --- |
| Knight Fortress | `outline-2.5d` | `warning` | 6 | 4 | 2201 ms | `3b192b33caf3d821335e0e0333b6c95d` |
| Knight Fortress Group | `outline-2.5d` | `warning` | 6 | 5 | 2158 ms | `e981180813d5313c1e135ff481bc1938` |

Both real-file UI cases completed in approximately 2.5 seconds, showed `已簡化模型`, downloaded a reconciled five-entry ZIP, and required no repair, axis, material, decomposition, next-step, or export-confirmation interaction. Group manifest metadata proved `removedComponentCount > 0`; project geometry contained exactly one finite CUT contour for every non-empty output layer.

## Verification

- `npm test -- --run`: 33 files, 874 tests passed.
- `npm run test:browser`: 4 files, 27 Chromium tests passed.
- Full E2E with both real-file environment variables and one worker: 11 passed, 0 failed, 0 skipped.
- `npm run validate:fixtures`: 10/10 accepted outcomes, including 8/8 automatic fixtures.
- `npm run test:performance`: 2/2 passed; 100k completed in approximately 3.8 seconds with no application long task at or above 100 ms after selection, and 500k failed safely in approximately 2.8 seconds.
- `npm run typecheck`: passed.
- `npm run build`: passed.
- `git diff --check`: passed.

## Remaining concerns

- Browser timings are machine-dependent; assertions retain the approved fixed resource/time limits and were not weakened.
- 2.5D remains an approximate largest-exterior workflow. The warning and safety documentation explicitly state that holes, internal detail, and smaller disconnected components are removed.

## Review follow-up

- Performance timing now starts immediately before `setInputFiles`, so the input change and browser file read are included. Test-generated large STL files use Playwright's per-test output directory and are never repository artifacts. The unchanged `<100 ms` long-task assertion passed; 100k completed in approximately 3.6 seconds and 500k failed safely in approximately 1.0 second.
- Removal evidence is now measured per layer. Projected layers receive `raster.componentCount - 1`; exact layers and exact aggregate are always zero. The public result aggregate must equal the per-layer sum.
- Canonical document, manifest, project JSON, SVG, DXF, PDF, and ZIP generation carry the aggregate and per-layer values. Validation rejects negative/non-integer values, a forged aggregate, a forged layer count, an aggregate/layer mismatch, and any non-zero exact evidence.
- Follow-up verification: focused domain/pipeline/export/worker 448/448, full unit 875/875, Chromium 27/27, real-file E2E 11/11 with zero skips, performance 2/2, typecheck/build/diff check passed.
