# ShapeCut 寬鬆工作台 UI 實作計劃

1. 在瀏覽器測試鎖定桌面主頁的留白、欄距及視窗內顯示要求。
2. 把有預覽的運算狀態改為大型預覽與右側狀態欄，並測試兩者不重疊。
3. 保留無預覽讀取狀態與手機版中央卡佈局。
4. 執行型別、單元、瀏覽器及建置測試，檢查 1280 × 720、1440 × 900 與 390 × 844。
5. 提交並推送至 `main`，等候 GitHub Pages 部署成功，再檢查公開網址。
