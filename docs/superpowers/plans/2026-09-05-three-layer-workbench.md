# 三層單視窗工作台實作計劃

> 使用 subagent-driven-development 分項實作、規格及品質審查。使用者已批准自主完成，不再逐項要求批准。

**目標：** 保留平面尺寸，原模型高度三等分取輪廓，輸出上中下三片；材料限定 6mm 壓克力、3mm 壓克力、6mm 木、3mm 木，預設 6mm 壓克力；結果使用單視窗分頁，三爪圓弧切割線須與下載一致。

**架構：** 保留現有轉換服務及 worker 邊界；材料目錄與製作厚度獨立於取樣高度。沿用現有配色字級。實驗 WASM 維持關閉，實物三爪相容性不作未驗證保證。

## 1. 材料選擇
- [x] 在材料及轉換器測試先加入四種材料、預設 acrylic-6、厚度 6/3/6/3 的斷言，執行並確認失敗。
- [x] 修改 geometry-estimates.ts 與 OneClickConverter.tsx；保留舊儲存專案可讀性，不刪除 IndexedDB 資料。新模型必須預選 acrylic-6，使用者仍可更改。
- [x] 執行材料、轉換器測試及 typecheck，審查後提交。

## 2. 三層
- [x] 在 layer-schedule.test.ts 加入高矮模型均三層、分段連續且覆蓋完整高度的斷言；先確認失敗。
- [x] 修改 outline 層排程與相應預算，追查精確路徑同樣使用三層；平面尺寸不縮放。組裝厚度使用選材，6mm 合計18mm、3mm合計9mm。
- [x] 驗證預覽、SVG/DXF/PDF、project/manifest 三層一致；更新原先六層最低值的測試，不放寬安全限制。

## 3. 單視窗
- [x] 瀏覽器測試先斷言 1440x900 及 1280x720 下頁面不縱向溢出、六下載可見及分頁可鍵盤切換。
- [x] 結果左側预覽，右側設定/提示/技術分頁，底部固定下載與重新選擇。詳細內容分頁，不隱藏必要警告；小視窗保留可達性，不裁切按鈕。
- [x] 保留字體、字級、配色；上載、處理、失敗、結果均驗收。

## 4. 三爪輪廓
- [x] 檢查現有模板三組曲線，量測不規則點及閉合、自交、旋轉對稱。以實際輸出而非預覽判定。
- [x] 在取得可支持的圓弧定義後先加入幾何回歸測試，再更新模板及版本；不得假造官方尺寸或沿用錯誤來源證明。
- [x] 檢查切割輪廓、保護距離、測試片與下載一致。若原始參考無法支持尺寸，明確指出並索取標示圖，不盲目磨平功能性缺口。

## 5. 驗收與發佈
- [x] Node、browser、typecheck/build、公開樣本端到端下載檢查及獨立審查。
- [ ] 全部通過後提交並部署 GitHub Pages，同步 Drive，保留既有未追蹤檔案；驗證公開資產及兩個樣本。
